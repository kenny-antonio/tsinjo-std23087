spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tsinjo_prod
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration/data-prod

---
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tsinjo_preprod
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration/data-preprod

